<!DOCTYPE html>
<html>
	<head>
		<title>Signo - <%= title %></title>
		<meta charset="UTF-8" />
		<link rel="icon" href="/static/icon.png" />
		<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" />
		<link rel="stylesheet" href="/static/style.css" />
	</head>
	<body>
		<div class="header">
			<img class="logo" src="/static/icon.png" alt="icon" />
			<p class="title">Signo</p>
			<p class="slogan">Signing service with PKCS#11 support</p>
		</div>
		<div class="menu">
			<div class="pages">
				<% if(user) { %>
					<a class="page" href="/users">Users</a>
					<a class="page" href="/tokens">Tokens</a>
					<a class="page" href="/engines">Signing Engines</a>
					<a class="page" href="/signees">Signees</a>
				<% } %>
			</div>
			<div class="account">
				<% if(user) { %>
					<p>Signed in as <span class="username"><%= user.name %></span></p>
					<a href="/auth/logout">Log out</a>
				<% } else { %>
					<a href="/auth/login">Login</a>
				<% } %>
			</div>
		</div>